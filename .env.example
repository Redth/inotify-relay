# Copy to .env and adjust to taste.

# Host port mapped to the container (8080 inside the container).
INOTIFY_RELAY_PORT=8080

# Container timezone — affects log timestamps and template {timestamp} renders.
TZ=America/Los_Angeles
